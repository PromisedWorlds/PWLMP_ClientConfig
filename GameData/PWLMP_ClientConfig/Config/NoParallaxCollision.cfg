// Disable Parallax scatter collision. If not, different collision settings could mean a craft inside a scatter would collide on one user's game but not the other.
@ParallaxGlobal:NEEDS[ParallaxContinued]:FINAL
{
    @ScatterSystemSettings
    {
        @collisionLevel = -1
    }
}
